package com.garslo.dolldelivery

class DeliverySolver(dataFile: java.lang.String) {
  type Vertex = String
  type Edge = Map[String, Any]
  type Graph = List[Edge]

  def solve(): List[String] = {
    List()
  }
}
